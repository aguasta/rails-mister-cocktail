<div class="bigcontainer">
<h1 class="container"> <%=@cocktail.name.capitalize  %>🍹</h1>

</div>


<h6 class="container"> Ingredients: 
<ul> <br>
  <% @cocktail.doses.each do |dose| %>
  <li> <STRONG> <%= dose.ingredient.name.capitalize %> </STRONG> : <%= dose.description %>
  <%= link_to(dose_path(dose), method: :delete, data: { confirm: "ARE YOU SURE ?"}) do %>
    <i> (delete) </i>
    <% end %>
  </li>
  <br>
  <%end  %>
  <br>
  </ul>
</h6> 

<br>
<div class="container">
<%= link_to("Click to Edit", edit_cocktail_path(@cocktail)) %> 
</div>

<div class="container">
<%= link_to("Click to add Ingredient", new_cocktail_dose_path(@cocktail)) %> 
</div>

<h4 class="container">
<a href="/cocktails">Back to all Cocktails</a>
</h4>

</div>

<% if @cocktail.photo.attached? %>
  <%= cl_image_tag @cocktail.photo.key, height: 300, width: 400, crop: :fill %>
<% end %>